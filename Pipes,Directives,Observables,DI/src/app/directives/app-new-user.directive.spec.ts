import { AppNewUserDirective } from './app-new-user.directive';

describe('AppNewUserDirective', () => {
  it('should create an instance', () => {
    const directive = new AppNewUserDirective();
    expect(directive).toBeTruthy();
  });
});
